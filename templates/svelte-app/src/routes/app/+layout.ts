// The interactive app shell (FE §routing): `ssr = false` — a client-rendered SPA served via the
// static adapter's fallback. Auth-gated, backend-driven screens live under here.
export const ssr = false;
